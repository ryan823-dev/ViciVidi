import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ViciVidi AI - B2B Sales Intelligence Platform",
  description: "AI驱动的B2B销售线索研究平台，帮你找到精准客户，自动丰富公司数据，验证邮箱联系方式。Powered by Caesar Engine。",
  keywords: ["B2B", "sales intelligence", "lead generation", "email verification", "company data", "ViciVidi", "Caesar Engine"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="zh-CN" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} font-sans antialiased`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}